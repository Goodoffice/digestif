@App = React.createClass

  componentWillMount: ->
    @

  componentDidUpdate: ->
    @

  componentWillUnmount: ->
    @

  onChange: (state) ->
    @setState(state)

  render: ->
    `<div className='app'>
      <h1>Digestif</h1>
     </div>`

